# 变更记录

本文件记录 acessh 的每次行为变更,遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式。

## [0.1.2] - 2026-08-16

### 新增

- 设备列表存储迁移为 TOML 会话文件(一个设备一个 `.toml`,对齐 AceShell 会话设计)
- 文件夹分层:设备按文件夹分组展示(可折叠)、文件夹新建/重命名/删除、设备可归属文件夹
- 全局密钥库:密钥以 JSON 独立存储(名称/创建时间),设备通过 `key_path` 引用,本机生成一次多设备共享
- 生成/导入密钥交互:密钥选择面板、Ed25519 生成与公钥复制
- 部署公钥到服务器(ssh-copy):密码登录后自动追加 `~/.ssh/authorized_keys`,幂等去重
- as9 会话包导入导出:会话树递归勾选 + 密钥勾选,导出密码 8~64 位且大写/小写/数字/符号至少三类,包体 PBKDF2-SHA256(10 万次)+ AES-256-GCM 加密,会话文件原样携带、密钥跨机合并
- 识别桌面端会话类型(sftp/vnc/rdp/x11 等),页面标注"移动端暂不支持"并可编辑

### 安全

- 密码类字段(设备密码、密钥私钥与口令)全部 AES-256-GCM 加密落盘(`enc:v1:` 格式),主密钥存于 `sessions/keys/master.key`;旧明文数据读取兼容

### 变更

- SSH 私钥认证连接不再要求输入密码(密钥已在服务器 authorized_keys 注册,仅缺账户时补账户)

### 修复

- 脚本编辑器在部分输入法(vivo 等)下弹出安全键盘的问题(本地补丁 CodeField 支持开启联想)
- 编辑弹窗修改会话名后保存报"设备不存在"的问题(会话文件随改名迁移)

## [未发布]

### 新增

- 初始版本:基于 kterm 的移动端远程登录工具
- SSH 连接(密码认证、私钥认证),支持导入 OpenSSH PEM 私钥与生成 Ed25519 密钥对
- Telnet 连接,自动检测 `login:` / `Password:` 提示并回填账户密码
- 串口连接(设备路径 + 波特率)
- 设备管理:会话名为主键,记录打开次数与最近登录时间
- 设备列表排序(创建时间/打开次数/最近登录,正序/倒序)与模糊搜索
- 快速添加设备(仅密码认证)与完整新建设备(支持私钥)
- 会话后台挂起与切换:退出时可选择关闭连接或后台挂起,支持记住本次操作
- 终端页:两排手机端快捷输入栏、系统键盘开关、后台会话列表、脚本执行、终端外观设置
- 脚本管理:增删改查,执行前内容预览确认
- 连接记录:每次连接成败与时间落库,可下拉刷新
- 全部数据存储于系统固定目录(应用支持目录)的 SQLite
