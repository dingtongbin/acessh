# 应用图标生成配置:以根目录 logo.jpg 为源,生成 Android/iOS 图标。
# 不使用 adaptive icon(Android 8+ 会把前景内缩导致 logo 变小),
# 改为传统全图图标,桌面显示 logo 铺满。
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "logo.png"
  min_sdk_android: 21
